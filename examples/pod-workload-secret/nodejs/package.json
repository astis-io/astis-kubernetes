{
  "name": "astis-pod-workload-secret",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RFC-020 workload-secret pod client (Node.js) — decrypt DB creds from ASTIS, connect to Postgres",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node main.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
